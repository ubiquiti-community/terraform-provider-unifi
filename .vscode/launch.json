{
    // Use IntelliSense to learn about possible attributes.
    // Hover to view descriptions of existing attributes.
    // For more information, visit: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Acceptance Tests",
            "type": "go",
            "request": "launch",
            "mode": "test",
            // the acceptance tests live in the unifi package, not the repo root
            "program": "${workspaceFolder}/unifi",
            "env": {
                "TF_ACC": "1",
            },
            "args": [
                "-test.v",
                // these tests spin up a UniFi container, so override the default 10m go test timeout
                "-test.timeout",
                "30m",
            ],
        },
        // You could pair this configuration with an exec configuration that runs Terraform as
        // a compound launch configuration:
        // https://code.visualstudio.com/docs/editor/debugging#_compound-launch-configurations
        {
            "name": "Debug - Attach External CLI",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            // this assumes your workspace is the root of the repo
            "program": "${workspaceFolder}",
            "env": {},
            "args": [
                // pass the debug flag for reattaching
                "-debug",
            ],
        }
    ]
}